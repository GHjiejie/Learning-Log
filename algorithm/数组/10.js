/**
 * @param {number} target
 * @param {number[]} nums
 * @return {number}
 */
// 长度最小的子数组
var minSubArrayLen = function (target, nums) {};

const nums = [2, 3, 1, 2, 4, 3];
const target = 7;
console.log(minSubArrayLen(target, nums));
